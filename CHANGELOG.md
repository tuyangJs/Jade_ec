# 更新日志

## v1.8 — 2026-06-26

### ✨ 新功能

#### 自定义配色（Coloris 色盘）

- 配色方案新增「自定义颜色」入口，点击弹出 **Coloris 色盘**（本地引入、**关闭透明度**、输出 hex）
- 所选颜色会推导出深 / 浅两套强调色变体（深色自动压暗约 18%），全站联动
- 色盘预设改为 **历史记录**：关闭色盘时记录所选色，最近在前、去重、最多 12 个，持久化保存

#### 背景材料新增「光线」(Side Rays)

- 移植自 reactbits 的 LightRays（原生 WebGL），光线**自左上角**射出
- 双色：`rayColor1` 取配色派生色、`rayColor2` 为白；跟随配色方案、深浅与「循环色」
- 透明叠加在页面底色之上，支持「页面背景透明度」

#### 配色方案新增「循环色」

- 选中后**整套强调色随色相循环**（按钮 / 激活态 / 高亮 / 聚焦环 / 极光 / 渐变 / 流光 / 光线全部跟随）
- 色板图标**自身循环变色**，直观表达用途
- 移除原独立的「背景色循环」开关

### 🎨 改进

- **激活态改为 iOS 磨砂玻璃片**：一级导航与二级侧边栏（列表 / 类树）统一，半透明填充 + 背景模糊，适配任意背景
- 激活项加 **对角渐变描边**（右上角 / 左下角高亮，描边色取强调色，深浅各一套）
- 页面背景透明度**最低值 20% → 10%**

### ⚙️ 技术

- 「循环色 / 极光 / 渐变」改由 **CSS 动画（`@property --cycle-hue`）** 驱动，全站强调色半透明统一改用 `color-mix(var(--color-primary) …)`；循环时**不再每帧改写 CSS**（流光 / 光线为 WebGL，由 JS 仅更新着色器颜色）

### 🐛 修复

- 修复浅色下激活项**对角描边「变形 / 断裂」**：浅色不再往白里调（会在浅色磨砂底上糊掉），改用鲜明强调色，渐变 stop 对称
- 「光线」浅色降速、加深、双色拉开，避免过浅 / 两色过近

### 📁 涉及文件

- `web/side-rays-bg.js`（新增）— 光线 WebGL 模块
- `web/vendor/coloris/*`（新增）— 本地引入的 Coloris 色盘
- `web/app.js`、`web/style.css`、`web/index.html`

***

## v1.7.1 — 2026-06-26

### 🐛 修复

- 修复「流光」背景在缩放 / 拖拽窗口时背景闪烁（黑屏）的问题
  - 改变 canvas 尺寸会清空 WebGL 绘制缓冲，而绘制循环被节流到 \~30fps，期间会闪一下黑
  - 现在窗口尺寸变化时立即补绘一帧；尺寸未变则跳过重设缓冲，避免无谓清屏

涉及文件：`web/funky-bg.js`

***

## v1.7 — 外观 / 主题大改

### ✨ 新功能

#### 配色方案

- 新增 **8 套预设强调色**：经典蓝 / 雅紫 / 桃粉 / 朱红 / 暖橙 / 翠绿 / 青碧 / 石墨
- 每套内置深 / 浅两套配色，自动适配系统深浅模式
- 设置 → 外观 提供圆形色板选择器
- 全站强调色（按钮、激活态、高亮、聚焦环、图标底等）统一联动，一键换色

#### 背景材料（由 3 种扩展为 6 种）

改为「搜索历史」风格的浮层下拉选择：

| 材料       | 说明                                      |
| -------- | --------------------------------------- |
| 默认       | 纯色界面底                                   |
| 渐变       | 跟随配色的静态斜向渐变（强调色 → 界面底色），深浅各一套           |
| 极光       | CSS 径向光晕，聚于左上 / 顶部缓慢呼吸漂移，透出到透明标题栏 / 侧边栏 |
| 流光       | 原生 WebGL 彩色渐变 + 噪声流动暗纹，跟随配色、深浅适配        |
| 云母 / 亚克力 | 非 Windows 11 自动禁用并标注「需 Win11」           |

#### 页面背景透明度

- 「默认」外所有材料均可调节
- 滑块控制详情面板透明度；**侧边列表始终更实**（透明量 = 详情 × 0.6），100% 时两者皆不透明、透出背景

#### 背景色循环

- 独立开关，仅 渐变 / 极光 / 流光 可用
- 让背景色彩虹循环（约 51 秒一圈），**界面强调色保持所选方案不变**

### 🐛 修复与优化

- 石墨方案的流光 / 渐变不再偏蓝（饱和度跟随强调色自身）
- 切换主题模式时，渐变 / 流光 / 极光背景实时跟随刷新
- 极光收窄并聚拢到左上，右侧不再出现多余光晕；呼吸动画更明显
- 深色强调色整体调深约 18%，深色下按钮 / 描边不再刺眼
- 激活导航项改为「实心强调色 + 白色图标」，在派生色背景下不再与背景撞色

### 🛠 构建

- `jade-ec-launcher` 与 `ec-parser` 改为**静态链接 CRT**，产物不再依赖 VC++ 运行库

### 📁 涉及文件

- `web/style.css` — design tokens、配色方案、各背景材料、下拉 / 色板样式
- `web/app.js` — 配色方案、背景材料下拉、派生色推导、背景循环逻辑
- `web/funky-bg.js`（新增）— 流光 WebGL 模块
- `web/index.html` — 引入 funky-bg.js
- `rust/.cargo/config.toml`、`rust/ec-parser/.cargo/config.toml`（新增）— `+crt-static`

